export const API_ENDPOINTS = {
  // Users
  ADD_USER: "/users/add",
  EDIT_USER: "/api/users/update", // use `/api/users/update/:id` in the call
  DELETE_USER: "/api/users/delete", // use `/api/users/delete/:id` in the call
  GET_USERS: "/users/all", // Endpoint to fetch all users
};
